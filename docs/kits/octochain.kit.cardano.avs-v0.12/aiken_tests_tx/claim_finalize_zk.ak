
test claim_then_finalize_with_zk() {
  // TODO: Claim as above
  // TODO: Create zk_ok reference UTxO (ZkDatum.ok=true) for event_id
  // TODO: Finalize: include zk_ok as reference input, no need for time-range lower bound at deadline
  expect True == True
}
